# Audio Module Test
This test can be run on nRF5340 Audio application or the `qemu_cortex_m3`.

To run the test on QEMU, run the following command:

zephyr/scripts/twister -T nrf/tests/subsys/a_mod -v
